import { AbstractControl, ValidationErrors, ValidatorFn } from "@angular/forms";

export class GanarSuperMoneda {

    user_id: string;
    tipo: string;
    credito: number;
    descripcion: string;
    empresa_id: string;
}